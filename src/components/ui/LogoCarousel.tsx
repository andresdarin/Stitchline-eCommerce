import { Quote } from "lucide-react";

export const LogoCarousel = () => {
    return (
        <div className="w-full h-32 flex items-center justify-center">
            <Quote size={40} className="" />
        </div>
    );
};
export default LogoCarousel;